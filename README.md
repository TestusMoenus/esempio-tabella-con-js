# programma che gestisce l'input in un oggetto e lo stampa a schermo su una tabella
dato in input un nome, cognome, eta e un hobby stampa in base all'array dinamico la tabella con campi e valori,
fatto js e html
